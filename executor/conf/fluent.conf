<source>
  @type exec
  command echo HelloWorld
  run_interval 3s
  tag executor
  <parse>
    @type tsv
    keys key1
    delimiter " "
  </parse>
</source>

<match executor>
  @type forward
  <server>
    host aggregator
    port 24224
  </server>
</match>